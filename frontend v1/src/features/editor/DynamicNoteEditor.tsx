/**
 * NoteEditor — eagerly loaded for offline-first readiness.
 * The Tiptap bundle loads with the initial page (no lazy loading / code splitting).
 */
export { NoteEditor } from "./components/NoteEditor";
